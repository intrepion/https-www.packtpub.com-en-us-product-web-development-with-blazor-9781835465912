@page "/admin/UrlNamePlaceholder-list"
@inject ICommentAdminService CommentAdminService
@attribute [StreamRendering]

<section>
    <h1>Comment List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    @* ListHeaderCodePlaceholder *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (TableLowercaseNamePlaceholder == null)
                {
                    <tr>
                        @* LoadingCountCodePlaceholder *@
                    </tr>
                }
                else if (TableLowercaseNamePlaceholder.Count == 0)
                {
                    <tr>
                        @* ZeroCountCodePlaceholder *@
                    </tr>
                }
                else
                {
                    @foreach (var comment in TableLowercaseNamePlaceholder)
                    {
                        <tr>
                            <td>@comment.Id</td>
                            @* ListItemCodePlaceholder *@
                            <td>
                                <NavButton Href="@("/admin/UrlNamePlaceholder-edit/" + comment.Id)" Name="Edit" />
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <NavButton Href="/admin/UrlNamePlaceholder-edit" Name="Create" />
    </article>
</section>

@code {
    List<Comment>? TableLowercaseNamePlaceholder = null;

    protected override async Task OnInitializedAsync()
    {
        TableLowercaseNamePlaceholder = await CommentAdminService.GetAllAsync();
    }
}

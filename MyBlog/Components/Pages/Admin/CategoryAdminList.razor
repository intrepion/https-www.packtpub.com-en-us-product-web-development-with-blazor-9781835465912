@page "/admin/UrlNamePlaceholder-list"
@rendermode InteractiveServer
@inject AuthenticationStateProvider GetAuthenticationStateAsync
@inject ICategoryAdminService CategoryAdminService
@attribute [StreamRendering]

<section>
    <h1>HumanNamePlaceholder List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    @* ListHeaderCodePlaceholder *@
                    @* <th scope="col">Title</th>
                    <th scope="col">ToDoList</th> *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (TableLowercaseNamePlaceholder == null)
                {
                    <tr>
                        @* LoadingCountCodePlaceholder *@
                        @* <td colspan="4">Loading...</td> *@
                    </tr>
                }
                else if (TableLowercaseNamePlaceholder.Count == 0)
                {
                    <tr>
                        @* ZeroCountCodePlaceholder *@
                        @* <td colspan="4">No to do items found.</td> *@
                    </tr>
                }
                else
                {
                    @foreach (var category in TableLowercaseNamePlaceholder)
                    {
                        <tr>
                            <td>@category.Id</td>
                            @* ListItemCodePlaceholder *@
                            @* <td>@category.Title</td>
                            <td>@category.ToDoList?.Title</td> *@
                            <td>
                                <a href="@("/admin/UrlNamePlaceholder-edit/" + category.Id)">Edit</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <a href="/admin/UrlNamePlaceholder-edit">Create</a>
    </article>
</section>

@code {
    List<Category>? TableLowercaseNamePlaceholder = null;

    protected override async Task OnInitializedAsync()
    {
        var authState = await GetAuthenticationStateAsync.GetAuthenticationStateAsync();
        var userName = authState.User.Identity?.Name;

        if (userName == null)
        {
            return;
        }

        TableLowercaseNamePlaceholder = await CategoryAdminService.GetAllAsync(userName);
    }
}

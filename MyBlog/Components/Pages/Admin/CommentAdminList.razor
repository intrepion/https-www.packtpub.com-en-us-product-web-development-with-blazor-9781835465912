@page "/admin/UrlNamePlaceholder-list"
@inject ICommentAdminService CommentAdminService
@attribute [StreamRendering]

<section>
    <h1>Comment List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    @* ListHeadersCodePlaceholder *@
                    @* <th scope="col">PropertyNamePlaceholder</th> *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Comments == null)
                {
                    <tr>
                        @* <td colspan="CountPlaceHolder">Loading...</td> *@
                    </tr>
                }
                else if (Comments.Count == 0)
                {
                    <tr>
                        @* <td colspan="CountPlaceHolder">No HumanLowerComments found.</td> *@
                    </tr>
                }
                else
                {
                    @foreach (var LowercaseNamePlaceholder in Comments)
                    {
                        <tr>
                            <td>@LowercaseNamePlaceholder.Id</td>
                            @* <td>@LowercaseNamePlaceholder.PropertyNamePlaceholder</td> *@
                            <td>
                                <NavButton Href="@("/admin/UrlNamePlaceholder-edit/" + LowercaseNamePlaceholder.Id)" Name="Edit" />
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <NavButton Href="/admin/UrlNamePlaceholder-edit" Name="Create" />
    </article>
</section>

@code {
    List<Comment>? Comments = null;

    protected override async Task OnInitializedAsync()
    {
        Comments = await CommentAdminService.GetAllAsync();
    }
}
